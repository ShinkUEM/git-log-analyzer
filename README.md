### \# Git Log Analyzer

###

### \## 📌 Описание проекта

### Git Log Analyzer — это Java‑приложение для анализа истории коммитов Git.  

### Программа умеет:

###\- считать количество коммитов по авторам и выводить топ‑N;

### \- искать коммиты по ключевым словам (например, `FIX:`, `BUG:`, `HOTFIX:`);

### \- выводить список всех уникальных авторов, отсортированный по алфавиту;

### \- формировать отчёт в формате \*\*JSON\*\* или \*\*PLAINTEXT\*\* (настраивается в конфиге).

### 

### Реализовано на \*\*Java 17\*\*, сборка через \*\*Maven\*\*.

### 

### ---

### 

### \## ⚙️ Конфигурация проекта

### Файл `app.properties` (лежит в корне проекта, добавлен в `.gitignore`):

### 

### ```properties

### \# Ключевые слова для поиска (через запятую)

### git.search.keywords=FIX:,BUG:,HOTFIX:

### \# Формат вывода: JSON или PLAINTEXT

### output.format=JSON



